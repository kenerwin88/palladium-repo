name: Deploy immutable artifact

on:
  workflow_call:
    inputs:
      credentials_environment:
        required: true
        type: string
      environment:
        required: true
        type: string
      expires_at:
        required: false
        type: string
        default: ""
      infrastructure_ref:
        required: true
        type: string
      run_id:
        required: true
        type: string
      run_attempt:
        required: true
        type: string
      source_sha:
        required: true
        type: string
      version:
        required: true
        type: string
    outputs:
      image_uri:
        description: Deployed immutable ECR digest URI
        value: ${{ jobs.deploy.outputs.image_uri }}
      url:
        description: Deployed environment URL
        value: ${{ jobs.deploy.outputs.url }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ inputs.credentials_environment }}
    concurrency:
      group: deploy-${{ inputs.environment }}
      cancel-in-progress: false
    permissions:
      actions: read
      contents: read
      id-token: write
    outputs:
      image_uri: ${{ steps.deploy.outputs.image_uri }}
      url: ${{ steps.deploy.outputs.url }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          ref: ${{ inputs.infrastructure_ref }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: deployable-${{ inputs.run_id }}-${{ inputs.run_attempt }}
          path: /tmp/deployable
          repository: ${{ github.repository }}
          run-id: ${{ inputs.run_id }}
          github-token: ${{ github.token }}
      - uses: aws-actions/configure-aws-credentials@e6de054238d6b7531b4efff3b6587d9aade6a06c # v6.2.3
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}
      - uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e # v4.0.1
        with:
          terraform_version: 1.14.8
          terraform_wrapper: false
      - name: Deploy and smoke test
        id: deploy
        env:
          AWS_REGION: ${{ vars.AWS_REGION }}
          ECR_REPOSITORY: ${{ vars.ECR_REPOSITORY }}
          EXPIRES_AT: ${{ inputs.expires_at }}
          RUN_ATTEMPT: ${{ inputs.run_attempt }}
          RUN_ID: ${{ inputs.run_id }}
          SOURCE_SHA: ${{ inputs.source_sha }}
          TARGET_ENVIRONMENT: ${{ inputs.environment }}
          TF_STATE_BUCKET: ${{ vars.TF_STATE_BUCKET }}
          VERSION: ${{ inputs.version }}
        run: >-
          ./scripts/ci/deploy.sh "$TARGET_ENVIRONMENT" "$VERSION" "$SOURCE_SHA"
          "$RUN_ID" "$RUN_ATTEMPT" /tmp/deployable/palladium-image.tar "$EXPIRES_AT"
